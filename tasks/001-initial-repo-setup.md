# Task 001: Initial Repo Setup

## Goal

Create the lightweight TypeScript proof-of-concept structure with documentation, placeholder schemas, parser stubs, normalizer stubs, CLI entry points, and passing baseline tests.

## Acceptance Criteria

- Requested files and directories exist.
- `pnpm test` passes.
- `pnpm typecheck` passes.
- `docs/IMPLEMENTATION_LOG.md` records what was created, commands run, results, limitations, and next step.
